name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: [ubuntu-latest, macos-latest]
    
    steps:
    - uses: actions/checkout@v1
    - name: cmake
      run: cmake .
    - name: make
      run: make
